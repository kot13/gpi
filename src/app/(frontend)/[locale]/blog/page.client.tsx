'use client'
import React from 'react'

/** Blog pages use the default light GPI header — no theme override needed */
const PageClient: React.FC = () => {
  return <React.Fragment />
}

export default PageClient
